"""Sort a deck of cards."""

import sys
sys.path.append('../../../lab/data-structures/')

import merge_sort


def sort_deck_of_cards(deck):
    """."""
    # res = merge_sort(deck)
    import pdb; pdb.set_trace()
